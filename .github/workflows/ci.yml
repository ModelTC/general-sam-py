name: CI
on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  format:
    name: Check Python format
    runs-on: ubuntu-latest
    steps:
      - &checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install ruff black
      - name: Ruff
        run: ruff check .
      - name: Black
        run: black --check --diff .
  rustfmt:
    name: Check Rust format
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - run: rustup update stable && rustup default stable
      - run: rustup component add rustfmt
      - run: cargo fmt --all --check
  tests:
    name: Run tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - *checkout
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install locally
        run: pip install -e ".[tests]"
      - name: Install additional dependencies
        run: pip install pytest-md pytest-emoji
      - uses: pavelzw/pytest-action@v2
        with:
          emoji: false
          verbose: true
          job-summary: true
      - name: Test building wheels
        uses: PyO3/maturin-action@v1
        with:
          sccache: true
          manylinux: auto
  linux:
    strategy:
      matrix:
        target:
          - arch: x86_64
            os: ubuntu-latest
          - arch: aarch64
            os: ubuntu-24.04-arm
    runs-on: ${{ matrix.target.os }}
    steps:
      - *checkout
      - &setup-python
        uses: actions/setup-python@v7
        with:
          python-version: "3.10"
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist --interpreter 3.10
          sccache: true
          manylinux: auto
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.target.arch }}
          path: dist
      - &pytest
        name: Run pytest
        shell: bash
        run: |
          set -e
          WHEEL="$(find dist -maxdepth 1 -type f -name 'general_sam-*.whl' -print -quit)"
          pip install "$WHEEL"[tests] --force-reinstall
          pytest --import-mode=importlib
  windows:
    strategy:
      matrix:
        target:
          - arch: x86_64
            os: windows-latest
    runs-on: ${{ matrix.target.os }}
    steps:
      - *checkout
      - *setup-python
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist --interpreter 3.10
          sccache: true
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-${{ matrix.target.arch }}
          path: dist
      - *pytest
  macos:
    strategy:
      matrix:
        target:
          - arch: aarch64
            os: macos-latest
    runs-on: ${{ matrix.target.os }}
    steps:
      - *checkout
      - *setup-python
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist --interpreter 3.10
          sccache: true
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-macos-${{ matrix.target.arch }}
          path: dist
      - *pytest
  sdist:
    needs: [tests]
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v7
        with:
          name: wheels-sdist
          path: dist
  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') && github.repository_owner == 'ModelTC' }}
    needs: [tests, format, rustfmt, linux, windows, macos, sdist]
    permissions:
      id-token: write
      contents: write
      attestations: write
      artifact-metadata: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - name: Generate artifact attestation
        uses: actions/attest@v4
        with:
          subject-path: "dist/*"
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
      - name: Upload to GitHub Release
        if: ${{ startsWith(github.ref, 'refs/tags/') && !contains(github.ref, 'dev') }}
        uses: softprops/action-gh-release@v3
        with:
          working_directory: dist
          files: |
            *.whl
            *.tar.gz
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}
  dependabot:
    name: Dependabot Auto-merge
    if: ${{ github.actor == 'dependabot[bot]' && github.event_name == 'pull_request'}}
    needs: [tests, format, rustfmt, linux, windows, macos, sdist]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    steps:
      - id: metadata
        uses: dependabot/fetch-metadata@v3
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
      - run: |
          gh pr review --approve "$PR_URL"
          gh pr merge --squash --auto "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
